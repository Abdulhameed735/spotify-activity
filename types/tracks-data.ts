import { Track, TrackAudioFeatures, TrackAudioAnalysis } from ".";

interface TracksData {
	tracksInfo: Track;
	tracksAudioFeatures: TrackAudioFeatures;
	tracksAudioAnalysis: TrackAudioAnalysis;
}

export type { TracksData };
